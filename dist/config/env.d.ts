import { SignOptions } from 'jsonwebtoken';
export declare const PORT: string, NODE_ENV: "test" | "development" | "production", MONGODB_URI: string, JWT_SECRET: string, ARCJET_ENV: "development" | "production", ARCJET_KEY: string, QSTASH_TOKEN: string, QSTASH_URL: string, SERVER_URL: string, EMAIL_PASSWORD: string;
export declare const JWT_EXPIRES_IN: SignOptions["expiresIn"];
